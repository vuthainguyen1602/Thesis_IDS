% SOICT 2026 — Springer LNCS template (English)
% Finalized submission-ready manuscript.
% Compile: ./compile.sh
%
% RESULT PLACEHOLDERS: empirical numbers are wrapped in \ph{...}. Fill them from
% papers/soict2026/results/benchmarks/summary.csv (edge benchmarks) and from the
% LEAK-FREE Exp7 run (results/ml_07_cross_method_comparison/cross_method_summary.csv).
% Do NOT reuse the legacy F1=1.000 numbers: they came from a configuration with
% destination_port leakage and feature-level duplicate flows, both now removed.
%
\documentclass[runningheads]{llncs}

\usepackage{fontspec}
\usepackage{polyglossia}
\setdefaultlanguage{english}
\setotherlanguage{vietnamese}
\input{../../../papers/latex/fonts-xelatex-vi.tex}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{xcolor}
\usepackage{url}
% Okabe-Ito colorblind-safe accents for TikZ diagrams
\definecolor{accBlue}{HTML}{0072B2}
\definecolor{accGreen}{HTML}{009E73}
\definecolor{accOrange}{HTML}{D55E00}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, shapes.geometric, fit, backgrounds, calc}

\newcommand{\ph}[1]{{\color{gray}\textit{[#1]}}}
% Tighter float packing (reclaim near-empty float pages)
\renewcommand{\topfraction}{0.92}
\renewcommand{\bottomfraction}{0.6}
\renewcommand{\textfraction}{0.06}
\renewcommand{\floatpagefraction}{0.85}
\setlength{\floatsep}{8pt plus 2pt minus 2pt}
\setlength{\textfloatsep}{10pt plus 4pt minus 3pt}
% No-op fallback: \babelshorthandoff is a babel command, undefined under polyglossia.
\providecommand{\babelshorthandoff}[1]{}

\graphicspath{{../../../results/ml_07_cross_method_comparison/}{../../../results/ml_08_anomaly_gate/}{../../../papers/soict2026/results/benchmarks/}{../figures/}}

\begin{document}

\title{A Distributed Real-Time Intrusion Detection System on a Jetson Orin Nano Super Edge Cluster using Kafka and PySpark}

\titlerunning{Distributed Edge IDS on Jetson Orin Nano Super}

\author{Thai Vu Nguyen\inst{1} \and
Dung Van Bui\inst{2,3} \and
Nhut Tri Do\inst{2,3} \and
Van Du Nguyen\inst{4}}

\authorrunning{T. V. Nguyen et al.}

\institute{University of Transport and Communications, Ho Chi Minh City Campus, Vietnam\\
\email{nvthai@utc2.edu.vn}
\and
Vietnam National University Ho Chi Minh City, Vietnam
\and
University of Information Technology, Vietnam\\
\email{dungbv.17@grad.uit.edu.vn, trinhutdo@uit.edu.vn}
\and
Faculty of Information Technology, Nong Lam University, Ho Chi Minh City, Vietnam\\
\email{nvdu@hcmuaf.edu.vn}}

\maketitle

\begin{abstract}
Deploying machine learning (ML) based intrusion detection on resource-constrained edge devices requires balancing accuracy, latency, and operational cost. We present a split-deployment IDS architecture across two NVIDIA Jetson Orin Nano Super devices connected through Apache Kafka. The system implements a two-stage pipeline: (1) a lightweight autoencoder anomaly gate on Jetson~\#1 filters benign traffic, and (2) a PySpark \texttt{PipelineModel} classifier on Jetson~\#2 processes only suspicious flows. Central infrastructure (Kafka, PostgreSQL, InfluxDB, Grafana) runs on a host PC. The classifier is the model selected by our companion Spark ML study under a \emph{leakage-aware} evaluation (label-leaking port features removed, feature-level duplicate flows eliminated). We evaluate three distributed modes (pipeline split, horizontal Kafka consumer scaling, and a Spark standalone cluster) while streaming CICIDS2017 network flows. We report throughput, latency, and the gate's recall--skip trade-off across the three deployment modes: decoupling the gate from the classifier raises verdict throughput from 2.6 to 62.5 flows/s ($24\times$) at a run-level p95 of $\approx$13\,ms, with the gate skipping 95.8\% of benign traffic.

\keywords{Intrusion detection \and Edge computing \and Jetson Orin Nano Super \and Kafka \and PySpark \and Stream processing.}
\end{abstract}

\section{Introduction}

Network intrusion detection systems (IDS) increasingly must operate closer to data sources to reduce bandwidth and response time~\cite{shi2016edge}. Edge devices such as the NVIDIA Jetson Orin Nano Super offer limited CPU, memory, and thermal headroom, making monolithic ML inference challenging. A practical edge IDS must therefore decide \emph{which} model to deploy (small, accurate, and explainable) and \emph{how} to distribute inference across nodes under streaming load.

\textbf{Contributions.}
(1) A two-stage distributed IDS combining a lightweight anomaly gate with a Spark-based classifier;
(2) Three deployment modes for a two-node Jetson cluster (pipeline split, horizontal scaling, Spark standalone cluster);
(3) An end-to-end performance evaluation reporting throughput, latency percentiles, and per-node resource usage, which are metrics that offline studies typically omit;
(4) Open, reproducible scripts integrated with a \emph{leakage-aware} Spark ML training pipeline, so the deployed model is the one validated under a rigorous offline protocol; all code and configurations are publicly available\footnote{\url{https://github.com/vuthainguyen1602/Thesis_IDS} (branch \texttt{develop}).}.

\section{Related Work}

Edge computing pushes analytics closer to data sources to reduce latency and bandwidth~\cite{shi2016edge,hamdan2020edge}. Prior edge IDS studies~\cite{sforzin2016rpids,baalaji2025ensemble,asghar2026hedid} benchmark single-node inference on resource-constrained boards, including recent NVIDIA Jetson deployments, but they rarely evaluate multi-node Kafka pipelines with explicit role separation. Streaming platforms such as Kafka~\cite{kreps2011kafka} decouple producers and consumers; however, most ML-based IDS papers~\cite{khraisat2019survey,ferrag2020deep,vinayakumar2019deep} report offline batch accuracy without end-to-end throughput or per-node resource traces.

\textbf{Split inference.} Anomaly-detection surveys~\cite{chandola2009anomaly,ruff2021unifying} motivate two-stage designs: a lightweight gate followed by a heavier classifier. Autoencoder-based detectors~\cite{lopez2017autoencoder,hasan2025iiot} and federated edge IDS~\cite{nguyen2019diot} explore similar ideas on servers or homogeneous edge nodes, and recently on a single Jetson Nano~\cite{hasan2025iiot}, but none use PySpark MLlib classifiers on the NVIDIA Jetson Orin Nano Super~\cite{nvidia2019jetson}. Our companion ML study selects a leakage-free SHAP Top-30 model on CICIDS2017~\cite{sharafaldin2018toward,lundberg2017unified}; this paper focuses on \emph{how} to deploy that model across two Jetsons with a measurable gate-skip ratio and three scaling modes. Table~\ref{tab:related-work} positions our system against representative related work.

\input{../../../papers/latex/related_work_table_soict.tex}

\section{System Architecture}

\subsection{Split Deployment Overview}

Figure~\ref{fig:architecture} shows the split-deployment architecture. A host PC runs Kafka, PostgreSQL, InfluxDB, and Grafana. Two Jetson Orin Nano Super nodes consume messages from the network-flow topic. In pipeline-split mode, Jetson~\#1 runs the anomaly gate and forwards only \emph{suspicious} flows to a dedicated suspicious-flow topic. Jetson~\#2 then applies the PySpark classifier and writes the classification results back to PostgreSQL. Both nodes stream performance metrics to InfluxDB/Grafana and raise alerts via Email/Slack (dashed arrows in Fig.~\ref{fig:architecture}).

\begin{figure}[t]
  \centering
  \resizebox{0.92\textwidth}{!}{%
  \begin{tikzpicture}[
      font=\small, >=Stealth, every node/.style={align=center},
      pc/.style ={draw=gray!60,        thick, rounded corners, fill=gray!8,  minimum width=13.4cm, minimum height=1.3cm, inner sep=3pt},
      jet/.style={draw=accGreen!80!black, thick, rounded corners, fill=accGreen!8, text width=4.2cm, minimum height=2.0cm, inner sep=4pt},
      mtr/.style={draw=accBlue,        thick, rounded corners, fill=accBlue!5,  minimum width=13.4cm, minimum height=1.0cm, inner sep=3pt},
      lbl/.style={font=\footnotesize, fill=white, inner sep=1.5pt, align=center, text=accBlue!75!black},
      arr/.style ={-{Stealth[length=2.5mm]}, thick, draw=gray!60!black},
      dsh/.style={-{Stealth[length=2.5mm]}, thick, draw=accBlue, dashed},
  ]
    \node[pc]  (pc)  at (0, 0.0)   {\textbf{Coordination server (PC, Docker)}\\[2pt]
        Apache Kafka \;$|$\; PostgreSQL \;$|$\; InfluxDB \;$|$\; Grafana \;$|$\; CSV flow replayer};
    \node[jet] (j1)  at (-4.6,-3.6) {\textbf{Jetson Orin Nano Super \#1}\\[2pt] Anomaly gate\\[2pt]
        Autoencoder filters out\\ benign traffic};
    \node[jet] (j2)  at ( 4.6,-3.6) {\textbf{Jetson Orin Nano Super \#2}\\[2pt] Classifier\\[2pt]
        PySpark \texttt{PipelineModel}\\ Benign / Attack classification};
    \node[mtr] (out) at (0,-6.9)   {Performance metrics $\rightarrow$ InfluxDB $\rightarrow$ Grafana \quad$|$\quad
        Alerts: Email (Mailtrap) / Slack};

    \draw[arr] (pc.south -| j1) -- node[lbl]{network-flow\ topic} (j1.north);
    \draw[arr] (j1.east) -- node[lbl]{suspicious-flow topic\ (suspicious flows)} (j2.west);
    \draw[arr] (j2.north) -- node[lbl]{classification results\\ $\rightarrow$ PostgreSQL} (pc.south -| j2);
    \draw[dsh] (j1.south) -- (out.north -| j1);
    \draw[dsh] (j2.south) -- (out.north -| j2);
  \end{tikzpicture}}
  \caption{Split-deployment architecture (PC + 2$\times$ Jetson Orin Nano Super).}
  \label{fig:architecture}
\end{figure}

\subsection{Pipeline Roles}

\begin{itemize}
  \item \textbf{Anomaly gate:} a lightweight autoencoder + scaler; the MSE threshold is calibrated offline as a quantile of a held-out \emph{benign validation split} (10\% of benign training flows, never the test set), so the operating point is committed before evaluation.
  \item \textbf{Classifier:} PySpark \texttt{PipelineModel} with the leakage-free SHAP-selected feature set.
  \item \textbf{Combined role:} gate and classifier in one process, used for horizontal scaling via Kafka consumer groups.
\end{itemize}

\subsection{Monitoring and Alerting}

Each node tags its metrics with a unique node identifier in InfluxDB and PostgreSQL. Alerts are emitted from a designated primary node only, avoiding duplicate notifications. Figure~\ref{fig:grafana} shows the live Grafana dashboard while CICIDS2017 flows are replayed through the split deployment: prediction throughput, detected-attack count, inference latency, and per-node CPU/RAM utilization are all tracked in real time, alongside the most recent attack alerts persisted to PostgreSQL. Figure~\ref{fig:grafana-peak} captures the dashboard at peak load: the gate node idles near 5\% CPU while the PySpark classifier node reaches roughly 91\%, confirming that the classifier dominates the compute budget.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.75\textwidth]{grafana_dashboard.png}
  \caption{Real-time Grafana monitoring of the running edge IDS: per-node throughput, detected attacks, inference latency, and CPU/RAM utilization for both Jetson Orin Nano Super nodes, plus recent PostgreSQL-backed attack alerts.}
  \label{fig:grafana}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.75\textwidth]{grafana_peak_load.png}
  \caption{Grafana dashboard at peak load in the split deployment: the anomaly-gate node (\texttt{jetson-nano-1}) uses about 5\% CPU while the PySpark classifier node (\texttt{jetson-nano-2}) reaches roughly 91\%, showing the load asymmetry between the two pipeline roles.}
  \label{fig:grafana-peak}
\end{figure}

\section{Implementation}

Training and model export run on a Spark standalone cluster (host PC as master, two Jetson Orin Nano Super devices as workers) using Apache Spark ML~\cite{zaharia2010spark}; the cluster topology is shown in Fig.~\ref{fig:spark-cluster}. The host only coordinates (Spark master) and does \emph{not} run an executor, so all reported training compute reflects the two homogeneous Jetson workers rather than the stronger host CPU. The edge stack is implemented in Python with configurable per-node roles. Every offline run logs its configuration (random seed, leakage/dedup switches, library versions) so each exported model is traceable to the exact pipeline that produced it.

\begin{figure}[htbp]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
      font=\small, >=Stealth, every node/.style={align=center},
      master/.style={draw=gray!65,        thick, rounded corners, fill=gray!8,   text width=12.6cm, minimum height=1.2cm, inner sep=4pt},
      work/.style  ={draw=accGreen!80!black,  thick, rounded corners, fill=accGreen!8,  text width=5.6cm,  minimum height=2.2cm, inner sep=5pt},
      store/.style ={draw=accBlue,         thick, rounded corners, fill=accBlue!5,   text width=5.6cm,  minimum height=0.8cm, inner sep=3pt},
      lbl/.style   ={font=\footnotesize, fill=white, inner sep=1.5pt, text=gray!35!black},
      dn/.style    ={-{Stealth[length=2.6mm]}, thick, draw=gray!55!black},
      up/.style    ={-{Stealth[length=2.6mm]}, thick, draw=accOrange, dashed},
  ]
    \node[master] (m) at (0,0) {\textbf{Coordinator: MacBook Air (Apple M3, 16\,GB)}\\[2pt]
        Spark \textbf{Master} (cluster coordination) \;$|$\; CSV\,$\rightarrow$\,Parquet preprocessing \;$|$\; Docker: Kafka / PostgreSQL / InfluxDB / Grafana\\[1pt]
        \textit{No executor, does not run training compute}};
    \node[work] (w1) at (-3.6,-4.0) {\textbf{Jetson Orin Nano Super \#1}\\[1pt]
        Spark \textbf{Worker} + \textbf{Driver}\\[2pt]
        Executor: cores=4, mem=3\,GB\\
        Data partitions\\
        swap 8\,GB (NVMe)};
    \node[work] (w2) at ( 3.6,-4.0) {\textbf{Jetson Orin Nano Super \#2}\\[1pt]
        Spark \textbf{Worker}\\[2pt]
        Executor: cores=4, mem=3\,GB\\
        Data partitions\\
        swap 8\,GB (NVMe)};
    \node[store] (s1) at (-3.6,-6.1) {Parquet (synced)};
    \node[store] (s2) at ( 3.6,-6.1) {Parquet (synced)};
    \draw[dn] (m.south -| w1) -- node[lbl]{tasks\\ + partitions} (w1.north);
    \draw[dn] (m.south -| w2) -- node[lbl]{tasks\\ + partitions} (w2.north);
    \draw[up] (w1.west) -- ++(-0.7,0) node[lbl,pos=1,anchor=east,xshift=-1pt]{results\\ (shuffle/collect)} |- (m.west);
    \draw[up] (w2.east) -- ++( 0.7,0) |- (m.east);
    \draw[dn] (w1.south) -- (s1.north);
    \draw[dn] (w2.south) -- (s2.north);
  \end{tikzpicture}}
  \caption{Apache Spark standalone training cluster: the host acts as \emph{master} (coordination only, no executor), while two Jetson Orin Nano Super devices are the \emph{worker}/\emph{executor} nodes; Parquet data is partitioned and synced to both, with results gathered via shuffle/collect.}
  \label{fig:spark-cluster}
\end{figure}

\section{From Spark Training to Edge Deployment}
\label{sec:bridge}

This paper does \emph{not} compare Spark against Jetson Orin Nano Super as competing platforms. The two components address complementary phases of the same train--deploy pipeline (Fig.~\ref{fig:train-deploy}). Spark answers \emph{which model and how many features} achieve acceptable accuracy on the full CICIDS2017 dataset~\cite{sharafaldin2018toward}, whereas the Jetson Orin Nano Super answers \emph{whether that model can run in real time} under memory, thermal, and bandwidth constraints at the network edge~\cite{shi2016edge}.

\begin{figure}[htbp]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
      font=\small, >=Stealth, every node/.style={align=center},
      stp/.style={draw=gray!60,        thick, rounded corners, fill=gray!8,  text width=2.6cm, minimum height=1.15cm, inner sep=2pt},
      jst/.style={draw=accGreen!80!black, thick, rounded corners, fill=accGreen!8, text width=2.6cm, minimum height=1.15cm, inner sep=2pt},
      band/.style={draw=gray!45, thick, rounded corners, inner sep=8pt},
      hdr/.style={font=\footnotesize\bfseries, text=gray!30!black},
      arr/.style={-{Stealth[length=2.2mm]}, thick, draw=gray!55!black},
      exp/.style={-{Stealth[length=2.6mm]}, very thick, draw=accBlue},
  ]
    % --- Offline row (Spark cluster) ---
    \node[stp] (p1) {Parquet\\flows};
    \node[stp, right=0.65cm of p1] (p2) {Leak-free\\ranking / PCA};
    \node[stp, right=0.65cm of p2] (p3) {Train 8\\classifiers};
    \node[stp, right=0.65cm of p3] (p4) {F1 + paired\\stats};
    \node[stp, right=0.65cm of p4] (p5) {Export\\\texttt{PipelineModel}};
    \draw[arr] (p1)--(p2); \draw[arr] (p2)--(p3); \draw[arr] (p3)--(p4); \draw[arr] (p4)--(p5);
    \begin{scope}[on background layer]
      \node[band, fill=gray!4, fit=(p1)(p5)] (offb) {};
    \end{scope}
    \node[hdr, anchor=south west] at (offb.north west) {Offline phase: Spark cluster (host PC master $+$ 2 Jetson workers)};

    % --- Edge row (2x Jetson) ---
    \node[jst, below=2.7cm of p1] (e1) {Kafka\\flows};
    \node[jst, right=0.65cm of e1] (e2) {Anomaly gate\\(Jetson \#1)};
    \node[jst, right=0.65cm of e2] (e3) {PySpark classifier\\(Jetson \#2)};
    \node[jst, right=0.65cm of e3] (e4) {Throughput /\\latency / CPU--RAM};
    \draw[arr] (e1)--(e2); \draw[arr] (e2)--(e3); \draw[arr] (e3)--(e4);
    \begin{scope}[on background layer]
      \node[band, fill=accGreen!4, fit=(e1)(e4)] (edgb) {};
    \end{scope}
    \node[hdr, anchor=south west, text=green!30!black] at (edgb.north west) {Edge phase: 2$\times$ Jetson Orin Nano Super};

    % --- model export link ---
    \draw[exp] (p5.south) -- ++(0,-0.95) -| node[pos=0.25, above=2pt, font=\footnotesize, text=accBlue!75!black, fill=white, inner sep=2pt]{export model} (e3.north);
  \end{tikzpicture}}
  \caption{Two-phase train--deploy pipeline linking Spark ML experiments to Jetson edge inference.}
  \label{fig:train-deploy}
\end{figure}

\subsection{Offline Phase (Spark ML), Leakage-Aware}

In our companion ML study, we evaluate four feature configurations (baseline with all features, RF Importance Top-30, SHAP Top-30, and PCA with $k$=40) under a fixed 80/20 train--test split, using eight Spark ML classifiers and statistical validation (repeated paired resampling and permutation tests). PCA uses $k$=40 rather than 30 because it \emph{extracts} components (linear combinations spreading variance across many axes) rather than \emph{selecting} original features, so it needs more components to retain comparable variance; across the common $\{20,30,40\}$ sweep, $k$=40 retains the most variance and is the fairest representative operating point for PCA. LightGBM is excluded because its SynapseML native libraries are x86\_64-only and cannot run on the ARM64 Jetson Orin Nano Super deployment target. Crucially, the offline protocol is \emph{leakage-aware}~\cite{arp2022dosdonts}: it removes information that would be unavailable at deployment time but that otherwise inflates test scores. Motivated by documented labeling and duplication errors in CICIDS2017~\cite{lanvin2023errors}, the label-leaking \texttt{destination\_port} feature is removed and flows that are identical on all feature columns are de-duplicated before splitting. This yields realistic F1 values (Table~\ref{tab:spark-edge-bridge}) rather than the near-perfect scores obtained when these leaks are left in place. Offline metrics constrain \emph{which} exported model is worth deploying; they are not directly comparable to edge throughput or latency.

\begin{table}[htbp]
  \caption{Bridge table: offline Spark selection criteria (leakage-free offline evaluation on CICIDS2017; values from the companion study's released outputs).}
  \label{tab:spark-edge-bridge}
  \centering
  \small
  \begin{tabular}{l c c c p{3.2cm}}
    \toprule
    Configuration & F1 & Features & Size (MB) & Edge rationale \\
    \midrule
    Baseline + XGBoost & 0.9971 & 77 & 2.32 & Reference accuracy \\
    SHAP Top-30 + XGBoost & 0.9970 & 30 & 0.003$^{\dagger}$ & Explainable; fewer features (deployed feature set) \\
    RF Top-30 + XGBoost & 0.9922 & 30 & 2.52 & Same feature count; embedded ranking \\
    PCA $k$=40 + XGBoost & 0.9939 & 40 & 3.82 & Unsupervised reduction \\
    \bottomrule
  \end{tabular}

  \vspace{2pt}
  {\footnotesize $^{\dagger}$Serialized on the training cluster, where the distributed model-saving step scatters the tree-data partitions across executors; KB-scale entries captured only driver-local metadata and under-report the true size (a 30-feature XGBoost is in reality $\approx$2.5\,MB, on par with RF Top-30, since ensemble size depends on trees/nodes rather than input features; with fewer split candidates trees may even need slightly more nodes than the baseline, cf.\ 2.52 vs.\ 2.32\,MB). The single-node export used for deployment measures sizes reliably (cf.\ Table~\ref{tab:engines}).}
\end{table}

\subsection{Edge Phase (Jetson Orin Nano Super)}

The exported \texttt{PipelineModel} (VectorAssembler, StandardScaler, classifier) is loaded on Jetson~\#2 with only the selected SHAP features, reducing assembly and inference cost relative to the 77-feature baseline. The deployed input vector contains 29 features: the SHAP Top-30 ranking includes \texttt{destination\_port}, which is excluded from the exported model as a label-leaking channel on CICIDS2017, consistently with the leakage-aware offline protocol. A lightweight autoencoder gate on Jetson~\#1 filters benign flows before they reach Spark inference, further lowering load on the classifier node. End-to-end edge evaluation measures throughput (flows/s), latency p50/p95 (ms), per-node CPU/RAM/temperature, attack-class F1, and gate-skip ratio, none of which the offline Spark experiments capture.

\subsection{Division of Evaluation Metrics}

Table~\ref{tab:metrics-split} clarifies which metric belongs to which phase. Mixing offline F1 with edge latency in a single ranking would conflate accuracy with operational feasibility.

\begin{table}[htbp]
  \caption{Metric split between offline Spark training and edge Jetson inference.}
  \label{tab:metrics-split}
  \centering
  \small
  \begin{tabular}{p{2.4cm} p{4.2cm} p{4.2cm}}
    \toprule
    Aspect & Spark (offline) & Jetson (edge) \\
    \midrule
    Primary goal & Classification accuracy & Real-time feasibility \\
    Main metrics & F1, AUC-PR, Precision/Recall & Throughput, latency p50/p95 \\
    Secondary metrics & Train time, batch pred.\ time, model size & CPU, RAM, temperature, gate skip \% \\
    Data & CICIDS2017 train/test split & Kafka replay at 100 flows/s \\
    Scale & Millions of flow records & 2 nodes, 8\,GB RAM each \\
    \bottomrule
  \end{tabular}
\end{table}

We deploy the SHAP Top-30 feature set on Jetson~\#2 with a Random Forest classifier (offline F1 0.9955, within 0.002 of the best XGBoost score in Table~\ref{tab:spark-edge-bridge}): it retains near-baseline F1 with substantially fewer features, and Random Forest is a native Spark ML estimator, so the exported \texttt{PipelineModel} loads on the ARM board without the additional native libraries that Spark's XGBoost integration requires. Section~\ref{sec:benchmark-results} then evaluates this choice under streaming load; the companion ML paper reports the full four-method comparison and drift analysis.

\section{Experimental Setup}

\subsection{Hardware and Software}

2$\times$ NVIDIA Jetson Orin Nano Super Developer Kit (6-core Arm Cortex-A78AE, NVIDIA Ampere GPU with 1024 CUDA + 32 Tensor cores, 8\,GB LPDDR5, 256\,GB NVMe SSD, $\sim$67 TOPS), host PC running Docker Compose, Wi-Fi LAN. PySpark 3.4.1, Apache Kafka 3.5 (Confluent Platform 7.5.0), InfluxDB and Grafana for metrics.

\subsection{Workloads}

CSV replay at 100 flows/s over CICIDS2017. Reported metrics: throughput, p50/p95 latency (per-node percentiles over raw per-flow samples; the cluster p95 is the worst node's p95, plus end-to-end send-to-verdict latency from producer timestamps under NTP-synchronized clocks), per-node CPU/RAM/temperature, attack-class F1, gate-skip ratio, and energy-per-inference (mJ, via \texttt{tegrastats}, idle baseline subtracted).

\subsection{Baselines}

Single-node full pipeline vs.\ distributed modes A (pipeline split), B (horizontal Kafka scaling), and C (Spark standalone cluster).

\section{Results}
\label{sec:benchmark-results}

We run each deployment mode through a host-side orchestration script that starts the corresponding pipeline roles on the Jetson nodes over SSH, drives the load, and collects per-node logs. Each configuration is measured over at least three repetitions preceded by representative warmup traffic (excluded from the measured window, so JVM/JIT warmup does not inflate early-batch latency); Table~\ref{tab:benchmark} reports mean~$\pm$~std across repetitions. All metric queries are aligned to the exact load window rather than a trailing time range. Latency percentiles are computed per node over the raw per-flow samples logged during the window, and pipeline throughput is counted as \emph{final verdicts per second} (a flow's verdict is issued either at the gate, for skipped flows, or at the classifier), which avoids double-counting forwarded flows in pipeline-split mode. Aggregated results are reported in Table~\ref{tab:benchmark}, with throughput and p95 latency across modes visualized in Fig.~\ref{fig:edge-modes}.

\begin{table}[htbp]
  \caption{Distributed deployment comparison on the 2-node Jetson cluster (CICIDS2017 replay at 100 flows/s; mean~$\pm$~std over $\geq$3 repetitions; p95 from raw per-flow logs, worst node). ``(inline)'': in the combined role the gate runs in-process and its skips are persisted as verdicts, so no separate skip ratio applies. Energy is \emph{total-board} energy per verdict: at this load the idle-subtracted (active) power delta was below the \texttt{tegrastats} noise floor ($<$0.15\,W), so the figure is dominated by board idle power and measures how effectively each mode amortizes it over verdicts. $\dagger$~Detection quality is mode-independent (identical exported models); offline attack-class F1 is reported in Table~\ref{tab:spark-edge-bridge}.}
  \label{tab:benchmark}
  \centering
  \small
  \resizebox{\textwidth}{!}{%
  \begin{tabular}{l c c c c c}
    \toprule
    Mode & Throughput (flows/s) & Latency p95 (ms) & Gate skip (\%) & Energy/inf.\ (mJ) & Attack F1 \\
    \midrule
    Single node & 2.60 $\pm$ 0.41 & 12.3 $\pm$ 3.0 & (inline) & 2490 & $\dagger$ \\
    A: Pipeline split & 62.5 $\pm$ 0.6 & 13.1 $\pm$ 2.6 & 95.8 & 178 & $\dagger$ \\
    B: Horizontal & 5.9 $\pm$ 1.4 & 21.6 $\pm$ 19.2 & (inline) & 2250 & $\dagger$ \\
    C: Spark cluster & 90.0 $\pm$ 25.6 & 23.9 $\pm$ 17.8 & 97.9 & 119 & $\dagger$ \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\textbf{Energy efficiency.} On-board power is sampled with \texttt{tegrastats} during each run (30\,s idle baseline, then the load window; for two-node modes the powers of both nodes are summed). At 100 flows/s the measured \emph{active} (idle-subtracted) power delta was below the sampling noise floor ($<$0.15\,W on every node), so Table~\ref{tab:benchmark} reports \emph{total-board} energy per verdict instead: essentially board power amortized over verdict throughput. This still separates the modes sharply: the gate-based modes (A and C) amortize the two boards' power over 24--35$\times$ more verdicts than the single node, cutting energy per verdict from $\approx$2.5\,J to 0.12--0.18\,J. Nevertheless, the figures should be read as a deployment-level (not model-level) energy metric.

Figure~\ref{fig:classifier-terminal} shows the PySpark classifier node processing replayed CICIDS2017 traffic in operation: it sustains a steady final-verdict throughput ($\approx$6 flows/s in this window), per-batch inference latency of a few milliseconds, and correct per-batch attack counts, while the per-node monitor reports live CPU/RAM/temperature.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.62\textwidth]{pipeline_terminal_jetson2.png}
  \caption{Classifier node in operation: per-batch attack detection, few-millisecond inference latency, and live per-node resource telemetry during CICIDS2017 replay.}
  \label{fig:classifier-terminal}
\end{figure}

\subsection{Inference-engine baseline (cost of the unified stack)}
\label{sec:engine-baseline}

We deploy a Spark \texttt{PipelineModel} on the edge for \emph{consistency with the distributed training phase}: the same stack trains on the cluster and serves on the device. To quantify the price of that unification, we benchmark the \emph{same} RandomForest (matched hyper-parameters) on the \emph{same} deployed SHAP-selected feature set (Sec.~\ref{sec:bridge}) through lighter single-node engines: scikit-learn (CPU, no JVM) and ONNX Runtime (CPU execution provider), in Table~\ref{tab:engines}. Accuracy is comparable by construction; the comparison targets inference cost. The JVM/Spark stack indeed incurs substantially higher inference cost than scikit-learn on the 8\,GB ARM board ($9\times$ lower throughput, $\approx$11$\times$ higher p95, roughly double the RAM share, and $\approx$25$\times$ the active energy per inference), and compiling the same forest to ONNX Runtime widens the gap dramatically: $\approx$7{,}900 flows/s at a p95 of 1.1\,ms, i.e.\ $348\times$ the deployed Spark throughput at $\approx$2\% of the per-inference energy of scikit-learn. Reporting this honestly motivates exporting the \texttt{PipelineModel} to ONNX/TensorRT for inference while retaining Spark for training; wiring the ONNX session into the streaming pipeline (and exploiting the GPU via TensorRT) is left as future work.

\begin{table}[htbp]
  \caption{Single-node inference cost on the same model/features, measured on the classifier node under identical board conditions (batch size 10; energy is \emph{active}, i.e.\ idle-subtracted, per inference at engine saturation).}
  \label{tab:engines}
  \centering
  \small
  \begin{tabular}{l c c c c}
    \toprule
    Engine & Throughput (flows/s) & p95 latency (ms) & RAM (\%) & Energy/inf.\ (mJ) \\
    \midrule
    PySpark (deployed) & 22.6 & 532 & 24.3 & 71.6 \\
    scikit-learn       & 204.4 & 49.8 & 13.3 & 2.91 \\
    ONNX Runtime       & 7870.8 & 1.1 & 15.7 & 0.06 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[htbp]
  \centering
  \IfFileExists{../../../papers/soict2026/results/benchmarks/edge_modes.png}{%
    \includegraphics[width=0.62\textwidth]{edge_modes.png}%
  }{%
    \fbox{\parbox{0.76\textwidth}{\centering\vspace{1.2cm}\ph{Edge-mode bars --- run plot\_edge\_modes.py}\vspace{1.2cm}}}%
  }
  \caption{Throughput and p95 latency across deployment modes.}
  \label{fig:edge-modes}
\end{figure}

The anomaly gate is a \emph{tunable} filter rather than a single point: sweeping its threshold trades attack recall against the fraction of benign flows offloaded from Spark (Fig.~\ref{fig:gate}). This lets an operator pick an operating point that fits the available edge budget.

\begin{figure}[htbp]
  \centering
  \IfFileExists{../../../results/ml_08_anomaly_gate/gate_operating_points.png}{%
    \includegraphics[width=0.6\textwidth]{gate_operating_points.png}%
  }{%
    \fbox{\parbox{0.74\textwidth}{\centering\vspace{1.2cm}\ph{Gate operating curve --- run ml\_08\_anomaly\_gate\_autoencoder.py}\vspace{1.2cm}}}%
  }
  \caption{Anomaly-gate operating curve: attack recall vs.\ gate-skip ratio (and FPR).}
  \label{fig:gate}
\end{figure}

\section{Discussion}

Pipeline split relieves Spark of most benign traffic, and the realized benefit scales with the gate-skip ratio ($\approx$96\% in our runs): decoupling the gate from the Spark classifier lifted verdict throughput from 2.6 to 62.5 flows/s ($24\times$) at essentially unchanged p95. Horizontal scaling of the \emph{full} role roughly doubles the single node (5.9 vs.\ 2.6 flows/s) but remains Spark-bound, because every node still runs the classifier in-process. The Spark standalone cluster variant reached the highest mean throughput (90 flows/s) but with large run-to-run variance ($\pm$26) and an extra host dependency (the Spark master), so pipeline split remains the recommended default; cluster mode is most useful when model or batch size exceeds single-node memory.

As summarized in Sec.~\ref{sec:bridge}, offline Spark experiments and edge Jetson benchmarks answer different questions. Feature reduction from 77 to 30 dimensions (SHAP Top-30) directly reduces \texttt{VectorAssembler} and \texttt{PipelineModel} cost on the Orin Nano Super, while the anomaly gate skips a large fraction of benign flows before Spark inference is invoked. Together, these design choices connect the accuracy-oriented conclusions of the companion ML study to a deployable, measurable edge architecture.

\subsection{Limitations}

Benchmarks are obtained on a two-node lab cluster over Wi-Fi; wired networking and larger clusters may shift latency tails. The anomaly-gate threshold trades attack recall against the gate-skip ratio, so we report it as an operating curve (Fig.~\ref{fig:gate}) and leave live-traffic re-calibration to future work. Each configuration is repeated at least three times with warmup excluded (Sec.~\ref{sec:benchmark-results}); run-to-run variance on a thermally constrained board is reported alongside the means. Latency percentiles are computed per node from raw per-flow samples, and the cluster-level p95 is conservatively the \emph{worst node's} p95; end-to-end latency (from producer send to final verdict) relies on NTP-synchronized clocks and excludes DB-write cost. Running PySpark on the JVM on an 8\,GB Jetson incurs substantial overhead: Spark is chosen for consistency with the distributed training phase, the overhead is quantified in Sec.~\ref{sec:engine-baseline}, and the $\sim$67-TOPS GPU remains unexploited by the CPU-only pipeline (TensorRT is future work). The energy-per-inference figures subtract a single pre-run idle baseline, so within-run drift in background power is not captured; in split mode they sum both nodes' active energy, and the gate-skip saving does not subtract the autoencoder cost incurred on all flows. Finally, results are tied to CICIDS2017 replay: live traffic and deliberately evasive (adversarial) traffic remain untested.

\section{Conclusion}

We presented a reproducible distributed edge IDS for Jetson Orin Nano Super clusters with Kafka streaming and PySpark inference, deploying a model validated under a leakage-aware offline protocol. Future work includes TensorRT optimization, live-traffic validation, and federated retraining under drift.

\begin{credits}
\subsubsection{\discintname}
The authors declare that they have no competing interests.
\end{credits}

\bibliographystyle{../../latex/splncs04}
\bibliography{references,../../latex/related_work}

\end{document}
